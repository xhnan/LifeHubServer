<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xhn.fin.books.mapper.FinBooksMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        name,
        description,
        owner_id,
        default_currency,
        cover_url,
        created_at,
        updated_at
    </sql>

    <!-- 查询用户加入的账本列表（通过成员表） -->
    <select id="selectByUserId" resultType="com.xhn.fin.books.model.FinBooks">
        SELECT DISTINCT
            <include refid="Base_Column_List"/>
        FROM fin_books fb
        INNER JOIN fin_book_members fbm ON fb.id = fbm.book_id
        WHERE fbm.user_id = #{userId}
    </select>

</mapper>