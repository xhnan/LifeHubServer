# ==============================================================
# 服务器 .env 模板 - 复制为 .env 并填入真实值
# 部署目录：/usr/local/lifehub/server
# 只需配置 Nacos 连接参数，其他配置在 Nacos 控制台维护
# 【重要】.env 文件不能提交到 Git！
# ==============================================================


# Docker 镜像（由 CI/CD 自动更新，无需手动修改）
DOCKER_IMAGE=ghcr.io/your-github-username/lifehubserver:latest

# ---- Nacos 连接信息 ----
# Nacos 与应用同处一台服务器，容器内需通过 host.docker.internal 访问宿主机
# 不能写 localhost（那是容器自身），docker-compose.yml 已配置好 host-gateway 映射
NACOS_SERVER_ADDR=host.docker.internal:8848

# 命名空间 ID（在 Nacos 控制台 -> 命名空间 中查看，留空使用 public）
NACOS_NAMESPACE=

# Nacos 认证（如果你的 Nacos 开启了认证）
NACOS_USERNAME=nacos
NACOS_PASSWORD=nacos

# Nacos 配置分组（默认 DEFAULT_GROUP，通常不用改）
NACOS_GROUP=DEFAULT_GROUP
