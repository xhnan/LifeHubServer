# ==============================================================
# 服务器部署环境变量模板文件
# 复制此文件到服务器部署目录，重命名为 .env，填入真实值
# 【重要】.env 文件不能提交到 Git！
# ==============================================================

# Docker 镜像（由 CI/CD 自动更新）
DOCKER_IMAGE=ghcr.io/your-github-username/lifehubserver:latest

# Spring Profile
SPRING_PROFILES_ACTIVE=prod

# ---- 数据库 ----
DB_HOST=your-db-host
DB_PORT=5432
DB_NAME=LifeHub
DB_USERNAME=your-db-user
DB_PASSWORD=your-db-password

# R2DBC URL（响应式数据库连接，与上面保持一致）
SPRING_R2DBC_URL=r2dbc:postgresql://your-db-host:5432/LifeHub
SPRING_R2DBC_USERNAME=your-db-user
SPRING_R2DBC_PASSWORD=your-db-password

# ---- Redis ----
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# ---- JWT ----
# 生成方式：openssl rand -base64 64
JWT_SECRET=your-very-long-and-secure-jwt-secret-key-at-least-256-bits
